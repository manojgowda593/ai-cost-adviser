# OpenAI API key used by ai_analyzer.py for cost analysis (step ⑤).
# Get one from https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-key-here

# Optional: override the model (defaults to gpt-4o).
OPENAI_MODEL=gpt-4o
